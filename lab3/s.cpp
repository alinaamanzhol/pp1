#include <iostream>
#include <cmath>

using namespace std;
int main(){
    long int x;
    cin >> x;

    int root = (sqrt(x));
    if(sqrt(x) == root) cout << "Yes";
    else cout << "No";








    return 0;
}